{% extends "layouts/_base.twig" %}
{% block main %}
    <main class="container p-4 mx-auto bg-gray-800 shadow md:flex">
        <div class="self-start w-full md:mr-4 lg:mr-6 md:w-1/2 lg:w-1/4">
            <h2 class="mb-4 text-2xl font-bold">User</h2>
            <div class="p-6 bg-gray-700 rounded shadow">
                <div class="mb-4 text-center">
                    <img class="inline border-2 border-gray-800 rounded-full" src="{{user.email|gravatar}}" alt="">
                </div>
                <div class="mb-4 text-lg">
                    <span class="text-gray-500">#</span>
                    <span>{{user.id}}</span>
                </div>
                <div class="mb-4">
                    <div class="text-gray-500">username</div>
                    <div class="text-xl">{{user.username}}</div>
                </div>
                <div>
                    <div class="text-gray-500">email</div>
                    <div class="text-xl">{{user.email}}</div>
                </div>
                {% if currentUserId == user.id %}
                <div class="mt-4">
                    <a class="block w-full px-4 py-1 font-semibold text-center text-white bg-teal-700 rounded-sm hover:bg-teal-500"
                    href="/users/{{user.id}}/edit"
                    >Edit User</a>
                </div>
                {% endif %}
            </div>
        </div>
        <div class="w-full mt-4 md:mt-0 md:w-1/2 lg:w-3/4">
            <h2 class="mb-4 text-2xl font-bold">{{user.username}}s playlists</h2>
            {% include "partials/_playlists_list.twig" %}
            {% include "partials/_pagination.twig" %}
        </div>
    </main>
{% endblock %}
